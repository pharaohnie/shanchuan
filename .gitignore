# Build outputs (see build.sh)
relay/relay-server
public/croc.wasm
public/wasm_exec.js

# GitNexus local index
.gitnexus/

# OS
.DS_Store

# 生产配置（含域名、IP、密钥）；使用 *.example 模板
config.yaml
deploy/coturn/turnserver.conf
